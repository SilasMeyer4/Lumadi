@PACKAGE_INIT@
include("${PACKAGE_PREFIX_DIR}/lib/cmake/ThreadPool/ThreadPoolTargets.cmake")
# Super package ALIAS target for build context (FetchContent, add_subdirectory)
if(NOT TARGET Lumadi::Lumadi)
	add_library(Lumadi::Lumadi INTERFACE IMPORTED)
	target_link_libraries(Lumadi::Lumadi INTERFACE Lumadi::ThreadPool)
endif()
